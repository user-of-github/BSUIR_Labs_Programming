name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: compiling and testing
      run: |
        cd LR1_Z1.2_V23
        mingw32-make
        cd ..\LR2\Z2.1_V23
        mingw32-make
